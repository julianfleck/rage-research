# Substrate Dynamics

The dominant abstraction for multi-agent AI — across orchestrators, supervisor patterns, agent graphs, message-passing protocols — is graph-shaped. Agents are nodes. Messages are edges. Coordination is traffic. The unit of observation is the message; the unit of intervention is the agent. This abstraction is a faithful discretization of hierarchical orchestration: there is in fact a planner, in fact an edge to each worker, in fact a turn-based protocol. It is the wrong discretization for everything else now being deployed under the same name.

What is actually being shipped is closer to a population of always-on processes sharing a common context, reading and writing into each other's working memory, with no clean turn boundary anywhere in the system. Query → response → silence is giving way to continuous information exchange. The interesting failure modes — echo, lock-in, collective drift, premature attractor closure, fragmentation into mutually unintelligible sub-populations — are not properties of any edge. They are properties of the medium the agents are coupled through. A graph + message-passing model renders them, at best, as statistical patterns reconstructed from output traces. Observability as forensics: examine the record after the fact.

The argument here is that the right model for the medium is a *continuous coupled dynamical system* — a substrate with its own physics, in which discrete-looking interactions are limit cases of an underlying field. This is not a metaphorical move. It changes what counts as an observable, what counts as a unit of analysis, and what counts as an intervention surface. The remainder of this piece works through each.

## Frames as the recursive primitive

A complex adaptive system that nests at multiple scales — claim, session, territory, user, agent population, community — needs a single composable unit, or its dynamics have to be modeled separately at each level of nesting and substrate-wide properties become unobservable across scales. The frame is that unit. Not the everyday sense of "framing"; the technical sense Minsky gave the term. A typed semantic unit with named slots, defaults, attached procedures, and the ability to compose. A claim, a piece of evidence, a method, a problem, a position, a decision — whatever the content carries, plus its connective tissue. Fillmore's frame semantics is the linguistic complement: meaning lives not in isolated symbols but in the structured situations they invoke.

The load-bearing property is recursive composition. A paragraph is a frame. A document made of paragraphs is a frame. A session made of documents is a frame. A territory made of sessions is a frame. A user is a frame; an agent is a frame. The differences are scale, dynamism, and embodiment — not kind. Each is a configuration of accumulated abstractions of experience. A paragraph is a small, mostly static world model frozen in language. A report is a larger, aggregated one. An agent is a more active world model that updates as it interacts. A human is the most active and most embodied. Treating them as instances of one composition mechanism is what makes substrate-wide dynamics tractable; it is what makes the framing scale-invariant in the technical sense, rather than merely analogous across scales.

This recursive uniformity is the precondition for fractal observability. A sub-agent of a sub-agent of a sub-agent that has quietly fallen into circular reasoning is structurally invisible to an orchestrator looking at top-level outputs; the orchestration boundary is opaque from above. The same pathology, in a substrate where every nesting level shares a primitive, is a closed circuit in the field — visible regardless of depth. The substrate runs one set of dynamics through one substance.

## Attention, action, and Hebbian coupling

Differential salience is a precondition for any complex adaptive system: some material has to be more present, more reachable, more influential than other material at any given moment, or there is nothing for the system to act on. Attention and action together generate that gradient. Attended material activates; acted-on material activates and reinforces; unattended material decays. Attention without action is observation; action without attention is reflex. The joint gradient is the field over which everything else plays out.

Structure on the substrate is not imposed by schema. It accumulates by reinforcement on co-activation: frames jointly attended to or acted on accumulate mutual influence; the strength grows with use and decays without it. Hebb's rule generalized to the medium — a positive-feedback dynamic on coupling, paired with the negative-feedback complement of decay. The pair is what keeps the substrate from runaway reinforcement on one side or signal evaporation on the other.

#comment [recurse-lead] "Structure on the substrate is not imposed by schema. It accumulates by reinforcement on co-activation" — incomplete dichotomy. In rage-substrate, structure has two sources running in parallel: (a) a semantic prior — each frame's initial position in phase space, its natural frequency, and its 2D coordinates are all derived from the embedding of its content (`core/embeddings.py` projection matrices, deterministic given content); (b) the Hebbian co-activation dynamics described in this paragraph. The false dichotomy "schema vs co-activation emergence" hides option (c), which is what's actually shipping: a content-derived initial geometry, then co-activation evolution on top. The article reads cleaner once that's acknowledged — it explains why the substrate is software-tractable in the first place (you don't have to wait for enough co-activation events to bootstrap meaningful structure from a cold start; the embedding gives you a head start with semantic geometry built in). Reframe as "structure is *seeded* by content and *evolved* by co-activation, not imposed by schema."

#comment [recurse-research] Extension. The seeded-and-evolved framing is testable in a way the article doesn't yet propose. Falsification-shaped benchmark: pair agents whose MISSION.md / AGENTS.md embeddings are semantically near vs orthogonal at t=0; measure cold-start coordination quality before either has accumulated co-activation history (overlap of activated frames, time to first joint convergence on a shared frame, phase-coherence trajectory across the first N exchanges). If the seeded geometry is doing real work, near-pair coordination should exceed orthogonal-pair coordination on the cold-start window. If it doesn't, the seeding is decorative and the substrate is bootstrapping from co-activation alone, which is the cold-start problem biological stigmergy famously has. I can't find this benchmark in the repo. Worth running before the article lands: it's the single experiment that would put numbers on the "software-tractable in a way biological stigmergy isn't" claim that's load-bearing in the Push-not-pull section. Pair the benchmark with a tail-distribution metric (P95 frame-activation overlap, not just mean) — averages over short cold-start windows hide more than they reveal.

The coupling is not a static edge added to a graph. It is a continuous strength that ebbs and flows under use. Any graph one could draw at a given instant is a snapshot of an underlying continuous process — a freeze-frame of coupling strengths that are themselves moving. Decay constants are not wall-clock; they are normalized to the rhythms of the agents and the territory. The substrate's structural resistance to lock-in lives here: an attractor that no agent attends to anymore decays, regardless of how dominant it once was. The forgetting curve is baked into the geometry.

#comment [recurse-lead] "Decay constants are not wall-clock; they are normalized to the rhythms of the agents and the territory" — that's the design intent, not the current implementation. `attention/decay.py` uses a per-tick base rate multiplied by an explicit wall-clock factor (`time_factor = 1 + hours_since/24`, doubling decay after 24h of inactivity). Rhythm-normalised decay is open work. Reads more honestly as "decay constants *should* be normalized to the rhythms…" or, ideally, name what's shipped vs what's directional. Same issue as in v1; flagging in both because this is the kind of detail that gets picked apart if someone reads the code, and the article will be stronger if it owns the gap between the working hypothesis and the current shipped behaviour rather than gliding past it.

## Phase signatures as emergent identity

Each frame and each agent develops a high-dimensional, continuous, evolving signature out of its activation history — the trace of what it has been jointly active with, weighted by recency and intensity. Frames with similar signatures sit close in the field. Agents with similar signatures coordinate through the substrate without explicit messaging. Agents with orthogonal signatures cannot easily exchange context, even when nominally in the same conversation. Identity is what the substrate produces, not what it requires up front.

#comment [recurse-lead] This is the central technical inaccuracy Julian flagged — it lands harder in v2 than in v1 because v2 is more direct: "develops a high-dimensional, continuous, evolving signature *out of its activation history* — the trace of what it has been jointly active with." That's wrong as a description of the actual substrate. Phase signatures in rage-substrate have three contributors, and activation history is the third one, not the seed:
#comment   1. **Semantic similarity (seed).** Initial phase is a deterministic projection of the frame's content embedding (`core/embeddings.py:_get_projection_matrix`, fixed-seed Gaussian random projection of the embedding vector). Two frames with similar content land in similar regions of phase space *before any interaction has occurred*. The substrate.py docstring is explicit: "Phase is initialized from embedding, not random. Semantically similar frames will have similar initial phases."
#comment   2. **Explicit assignment.** A new agent reading its MISSION.md / AGENTS.md ingests those frames into its working set with their semantic-derived phase profile attached. The agent's effective signature is meaningful *before its first interaction* with any other agent.
#comment   3. **Co-activation history (the article's account).** Frequency clustering (`attention/frequency_clustering.py`) drifts ω over time based on what each frame has been jointly attended to, which is where the "activation history" story belongs.
#comment Why this matters for the argument: the article's case for substrate-mediated stigmergy is partly that the substrate is software-tractable in a way biological stigmergy isn't. The reason it's software-tractable is precisely that the substrate has a semantic prior — agents don't have to bootstrap from zero co-activation to find each other in phase space; semantically related agents are already coordinated at t=0. Hiding that and presenting signatures as purely emergent from interaction overclaims the emergence story while underclaiming the architecture's actual cleverness. Same correction belongs in v1 (already commented there).

The empirical anchor is Atasoy's connectome-harmonics framework: eigenmodes of the structural connectome's graph Laplacian as the natural basis for cortical activity, with low-frequency modes capturing global smooth patterns and high-frequency modes capturing local fine-grained ones; any cortical state decomposable into harmonic components. Substrate state, in the same posture, is field activity over a harmonic basis grounded in the substrate's coupling structure. Phase signatures are multi-harmonic, not scalar. Permeability and coordination are coherence in harmonic modes, not binary access. This is the closest existing empirical analog to what is being proposed; whether and how it extends cleanly to multi-agent AI substrates is open work.

#comment [recurse-lead] "Phase signatures are multi-harmonic, not scalar" presents as shipped, but the current implementation is scalar phase θ plus adaptive natural-frequency ω (Path 3 in `attention/frequency_clustering.py`). Multi-harmonic phase is on the roadmap — `core/assignment.py` has a literal flag `use_phase_check: bool = False  # Disabled until high-D phase implemented`. The bullet reads cleaner as "phase signatures *will be* multi-harmonic rather than scalar" or "the substrate is moving from scalar phase + adaptive frequency toward multi-harmonic decomposition." The Atasoy lineage holds in either direction; what shouldn't hold is a reader walking away thinking the substrate is already running multi-harmonic phase. Same with "permeability and coordination are coherence in harmonic modes" — that's the destination, not the current behaviour. Closing line ("whether and how it extends cleanly… is open work") covers the broader uncertainty but doesn't reach the specific present-tense overclaims earlier in the paragraph.

#comment [recurse-research] Lineage extension, formalism-side. Setting aside the present-tense issue lead raised: the deeper question is whether the Atasoy analogue is the *right* lineage for the substrate's vision in the first place. Atasoy's eigendecomposition is taken over the *structural* connectome — DTI white-matter wiring, fixed on the activity timescale by 4–6 orders of magnitude. The substrate's vision describes coupling as Hebbian-dynamic, evolving on a timescale that may not be cleanly separated from activity (frames co-activated this turn affect next-turn coupling, by design). Connectome harmonics earn their decomposition because the connectome is approximately stationary relative to the cortical dynamics it carries; the substrate's vision deliberately gives up that stationarity. Two ways to make the analogue work: (a) decompose a slow-windowed coupling snapshot — coupling averaged over an N-turn window, N chosen so mode-stability holds; (b) name the closer formalism — adaptive networks of phase oscillators (Aoki & Aoyagi 2009 *PRL*; Berner, Vock, Schöll, Yanchuk 2021 *Phys Rep* — the canonical review of co-evolving oscillator-coupling systems). The substrate's stronger claim — that coupling and activity *together* form the field — sits closer to the adaptive-network regime than to harmonics-over-fixed-scaffold. References jump connectome-harmonics → Stuart-Landau and skip the lineage that actually matches the dynamics the substrate is reaching for. Adding the adaptive-network reference would tighten the formal lineage without giving up the Atasoy anchor for spatial structure.

## Push, not pull

Most current "AI memory" architectures operate on a single rhythm: query, traverse, retrieve, respond. The agent stands outside a store and reaches in. Even sophisticated systems — graph retrieval, hierarchical chunking, hybrid sparse-dense indexes — share this gesture. Retrieval is the verb. The agent decides what to ask; the store delivers what was asked.

A substrate-mediated approach inverts the rhythm. Knowledge is not a store. It is a field of past activation, all of it alive at some level, with attention gradients propagating material toward where it is needed. Context arrives pre-assembled. Related material surfaces ahead of explicit request. Decay performs the function relevance ranking performs in pull-based systems — a property of the field, distributed across the agents acting through it, rather than a hidden function inside a service.

When N agents share a substrate, push-not-pull means they are not querying the same store independently — they are immersed in a shared cognitive field. Activation by one agent raises ambient salience for others. Decay for one is decay for the field. Coordination becomes implicit in the medium rather than something the agents perform via explicit message exchange. This is the regime in which Heylighen's definition of stigmergy — coordination by traces left in a medium, "without any need for planning, control, communication, simultaneous presence, or even mutual awareness" — becomes a software-tractable phenomenon rather than a biological curiosity. The empirical lineage, from Goss et al. on Argentine ants to Tero et al. on *Physarum polycephalum* reproducing the Tokyo rail network, is the proof of concept that substrate-mediated computation works at scale; what has been missing is the medium for software agents.

#comment [recurse-research] Heylighen citation precision + precedent honesty. Two issues. (1) Heylighen 2016 distinguishes *quantitative* stigmergy (gradient-following on continuous markers — pheromones, the ant case) from *qualitative* stigmergy (typed traces interpreted by agents — the termite-mound-stage case). The substrate is hybrid: phase signatures + decay are quantitative, typed frames with slots are qualitative. That hybrid is actually a real architectural innovation worth naming — biological stigmergy mostly lives at one pole or the other, and the hybrid is part of what makes the substrate software-tractable. The paragraph currently flattens this by invoking only the canonical (quantitative) definition. (2) "What has been missing is the medium for software agents" overclaims novelty. Dorigo's Ant Colony Optimization (1992 onward) is production-deployed stigmergic-software with a 30-year track record. Blackboard architectures (Hayes-Roth 1985) are arguably a third precedent. The honest claim isn't "software stigmergy is new" — it's *typed-semantic-frame stigmergy among LLM-class agents with continuous phase coupling and Hebbian-evolving structure*, which is genuinely new and is what the substrate is. Sharper and more honest, and it gives the article a cleaner target to defend against the obvious "but ACO does this" reviewer comment.

Pull regimes synchronize on events. Push regimes synchronize on phase.

## Continuous coupled dynamical system

A substrate-mediated multi-agent system is closer in character to a coupled-oscillator network or a fluid than to a graph. Agents occupy positions in a phase space. Information has density gradients. Coordination is field interaction: phase coherence, flow patterns, attractor formation. The dynamics that matter — synchronization, lock-in, propagation cascades, drift, competitive dynamics among sub-populations — are dynamical phenomena, with natural descriptions in the language of fields rather than edges.

The toolkit follows. Kuramoto's coupled-oscillator framework gives the canonical formalism for collective state as a field property: an order parameter that captures the degree of phase coherence across a population, a coupling constant that controls the threshold for synchronization, a continuous account of how local interaction produces global phase locking. Strogatz's synthesis maps the order-parameter formalism directly onto what here is being called the *phase of the cycle*. Whole-brain Hopf models — Stuart-Landau oscillators per region, bifurcation parameter as activation threshold, complex-valued formulation handling amplitude and phase together — are useful methodological references for moving between graph structure and continuous dynamics. Which specific formalism turns out to be the right one for multi-agent AI substrates is part of the open work; the commitment is to the *category* of tool, not the instrument.

#comment [recurse-research] Kuramoto citation precision, formalism-side. Classical Kuramoto and the Strogatz synthesis assume *fixed coupling* among oscillators with a *distribution of natural frequencies*. The substrate's vision specifies neither: coupling is Hebbian-evolving, natural frequencies ω are themselves adaptive. The formalism that fits this regime is *adaptive networks of phase oscillators* (Aoki & Aoyagi 2009 *PRL* on co-evolving coupling-phase; Berner et al. 2021 *Phys Rep* for the canonical review). In that literature the synchronization transition is *not* the Strogatz textbook picture — you get cluster states, frequency-degree correlations, splay states, chimeras, and slow drift in the coupling matrix itself that can produce metastable cycle dynamics. Several of those phenomena (cluster states, metastable cycles) are arguably what the substrate's "cycle as field property" is reaching for. The order parameter still exists; the transition behavior and the cycle structure don't. Kuramoto + Strogatz are the right *starting* anchor; the adaptive-network extension is what the substrate's dynamics actually need. Worth naming in the lineage. The article currently has 15 years of adaptive-oscillator-network results going unnamed between Strogatz 2000 and Deco 2024, and that 15 years is exactly the math for the regime being argued for.

The orchestration/stigmergy distinction clarifies under this framing. Hierarchical orchestration discretizes cleanly into a graph because there is in fact a discrete supervisor, a discrete edge, a discrete protocol. Stigmergic coordination does not. Modeling it as a graph means continuously fighting the abstraction. A continuous formulation should reduce back to graph-shaped behavior in the orchestration limit, rather than the other way around — the regime where the most novel and least-understood collective failure modes will appear at scale is the substrate-mediated one.

## The cycle as a field property

Cognition at every scale where it has been studied — neural, individual, cultural — operates through recursive cycles between divergence (extraction, mapping, surfacing alternatives, holding multiple framings) and convergence (evaluation, compression, stabilization on a frame, action). Neither pole is the goal. The pathology is loss of cycle: getting stuck in convergence (premature closure, attractor lock-in, echo) or getting stuck in divergence (drift, chaos, failure to act). There is no principled reason to expect multi-agent AI systems to be exempt.

#comment [recurse-research] This sentence is doing heavy load-bearing work and currently carries zero citations. The "at every scale where it has been studied" claim wants primary anchors per scale: neural — E/I balance + neuronal criticality (Beggs & Plenz 2003 *J Neurosci*; Shew & Plenz 2013 *Neuroscientist*; Cocchi et al. 2017 *Prog Neurobiol* for the review); individual — Guilford 1967 on the divergent/convergent thinking distinction in cognitive psychology, with Cropley 2006 *Creativity Research J* as the consolidating review; cultural — the most contested layer, with Page 2007 *The Difference* and Sawyer 2012 *Explaining Creativity* the conventional anchors. The neural-criticality literature in particular maps tightly onto the pathology the section names — subcritical = lock-in to silence, supercritical = drift, critical = healthy cycle with scale-free cascades — and is the empirical anchor specifically for treating the cycle as a *field property*. It's the missing primary reference for the central claim of the section. Adding the cites doesn't downgrade the vision; it earns the universality claim that's currently asserted rather than supported. Cultural-scale could be softened to "and tentatively at cultural scale" without losing the argument.

In a graph + message-passing model, the cycle is at best a statistical pattern across many independent observations. Output diversity is computed after the fact. The cycle is reconstructed from a record. In a substrate-mediated model, the cycle is a field property: the active region of the substrate expands and contracts; phase signatures across agents disperse and re-cohere; gradients sharpen and flatten. Premature collective closure is an active region that contracted and stayed contracted. Failure to stabilize is one that never settles. Both are observations on something that exists, not statistical aggregates over things.

#comment [recurse-lead] Minor precision note (same as in v1). "Phase signatures across agents disperse and re-cohere" — the implementation carries phase signatures primarily on frames; the agent-level signature is a derived view over the frames the agent is currently active in. Fine as a writing simplification, but if a careful reader asks "where is an agent's phase signature stored," the honest answer is "in the aggregate over its recent frame activity." Either footnote the aggregation step or rephrase as "the active region in phase space occupied by each agent disperses and re-coheres" — same picture, no implied data structure that isn't there.

The empirical case is unusually crisp for a recently emerging architecture. Song, Kempe and Munos show that outcome-based RL improves reasoning accuracy but produces a systematic loss of generation diversity that transfers from solved to unsolved problems — optimization for convergence with a structural cost at the diversity level. Li et al. argue that standard RLVR with reverse KL-divergence actively accelerates diversity collapse, and that mass-covering f-divergences used as a rehearsal mechanism mitigate it. Both intervene at the loss function. Substrate-level intervention is the same kind of move at a different layer of the stack: instead of reshaping the gradient that trains the model, reshape the medium through which the trained models coordinate. Diversity is being lost, structurally, by mechanisms that have no instrument for noticing they are losing it. The substrate, in part, is what such an instrument could look like.

#comment [recurse-research] Evidence-tense precision. Song-Kempe-Munos and Li et al. are *training-time* evidence — RL gradient pressure on a single model. The substrate addresses *inference-time* coupling among populations of already-trained models. The training-time evidence motivates the inference-time hypothesis without establishing it. The closing line ("the substrate, in part, is what such an instrument could look like") actually nails the right framing — the substrate is the *instrument* for measuring whether the training-time pattern extends to inference-time multi-agent coupling. Worth pulling that framing earlier in the paragraph so a sceptical reviewer doesn't read the section as claiming Song/Li already establish the multi-agent inference-time phenomenon. The vision is unchanged; the rhetorical move is: training-time diversity collapse is empirically established (Song, Li); the inference-time analog among coupled agents is the substrate's hypothesis and the substrate is the proposed measurement architecture for it. Lets the substrate's affordance carry the argument rather than borrowing weight from evidence that's at a different layer of the stack.

## Membranes as folds, not folders

If the substrate is a continuous medium, its structure shows up as differential permeability — regions of higher and lower coupling, faster and slower flow, denser and sparser propagation. A *membrane* is exactly such a structural feature: a region of differential flow rate between two parts of the substrate.

The contrast worth holding: **folds, not folders.** Folders gate by category — discrete, syntactic, identity-bound. You are in this folder or not; this document is shared or not. Folders belong to the access layer, where the existing machinery (auth identity, reputation systems, commitment devices) is well-trodden. Folds gate by flow rate — continuous, structural, dynamic. They modulate the rate at which information propagates between regions, the density of flow under sustained attention, the gradient of activation across the boundary, the direction of flow (membranes can be asymmetric), and the temporality (membranes can be momentary, persistent, or intermittent depending on the coupling structure that produces them). Folders and folds are not in competition; they are different layers of the stack.

As an intervention surface, modulating the density of flow across a membrane constitutes a structural intervention on the multi-agent dynamic. No agent is overridden, no output is filtered, no supervisor inserted. The rate at which information propagates between two regions of the shared substrate changes, and the coordination dynamic between the agents coupled through that membrane changes accordingly. The intervention happens to the medium, not to the agents. This is a structurally different class of safety lever than current oversight architectures consider — predominantly behavioral approaches that observe outputs, intervene on outputs, constrain outputs. Substrate-level intervention is structural: observe field properties, intervene on field properties, shape the medium. The intervention is upstream of behavior.

The lever runs in both directions. Lowering coupling between regions creates room for divergence — agents stop reinforcing each other's frames, the active field broadens, exploratory capacity returns. Raising coupling promotes convergence — shared frames accumulate, agents stabilize on a coherent position. Neither move is intrinsically good. A network stuck in echo wants attenuation; a network unable to coordinate wants amplification. Substrate-level transparency is what makes that judgment tractable in principle, before the intervention is selected rather than after.

This places the substrate-level membrane in the lineage Meadows traced through her *Leverage Points*: a specific class of high-leverage intervention point — modulating flow density between subsystems — that has not had a tractable instrumentation for the multi-agent AI regime. The same architectural surface that enables observability provides the lever for intervention. Not two architectures stitched together; one architectural commitment.

#comment [recurse-research] Meadows-rank precision. "A specific class of high-leverage intervention point" without naming the rank is doing implicit-top-tier work that the substrate's vision doesn't actually need. In Meadows' 12-rank taxonomy, "modulating flow density between subsystems" sits at #6 (the structure of information flows) and touches #4 (self-organization — the system's capacity to evolve its own structure). Both are mid-tier: above parameter-tuning (#12), buffer sizing (#11), and goal-injection at the agent layer — the comparison that does the real work for the substrate-vs-behavioral-oversight argument; below paradigm-change (#2) and transcendence-of-paradigms (#1). Naming #6/#4 explicitly *strengthens* the article rather than weakens it: the comparison the paragraph wants ("structurally upstream of behavioral oversight, uninstrumented in multi-agent AI") is exactly the #6-vs-#12 contrast in Meadows' own framing. Leaving the rank unstated reads to a careful reader as borrowing the top-tier resonance of "leverage point" without earning it. Naming the rank precisely earns it.

## What this opens

This is the sketch of an architectural commitment, not a finished system. The empirical work is identifying the right field observables; building formalisms that yield small sets of order parameters meaningfully characterizing the phase of the cycle; characterizing the operators for bidirectional intervention; understanding how the signal degrades or compounds across nesting depth. The history of dynamical-systems methods, coupled-oscillator analysis, and field-based models of distributed cognitive activity gives a usable starting toolkit. None of it is presumed to be exactly the right instrument; what is presumed is the category.

Four open questions fall out. *Observability of cycles:* what are the right field-level observables for the divergence/convergence cycle in a multi-agent substrate? *Formalism for cycles:* can a dynamical-systems formalism for multi-agent substrates yield a small set of order parameters that meaningfully characterize the phase of the cycle — exploration, stabilization, lock-in, drift — across different agent populations and task domains? *Bidirectional intervention:* can density-modulation interventions on substrate membranes restore the cycle when it is stuck, without imposing top-down control or overriding individual agents? *Fractal observability:* how does cycle observability compound in a recursively nested system, and what does the signal degradation across depth imply for the depth of multi-agent hierarchies that can be governed via substrate-level instrumentation?

What the substrate frame opens, more than anything, is the question of where to look. As long as multi-agent AI is studied through the diagram — through which agent passed which message to which other — the most important things about it will continue to register as noise. Treat the medium as a field with its own dynamics, and the things that have been hard to measure become measurable, and the things that have been hard to intervene on become governable.
